<% provide(:title, @user.user_name) %>

<div class="container">
  <div class="notification-bar">
    <%  if @notifications.exists? %>
      <%= render @notifications %>
    <% else %>
      <div>通知はありません</div>
    <% end %>
  </div>
</div>